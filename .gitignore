# Godot 4
.godot/
.import/
export.cfg
# export_presets.cfg IS committed (template; secrets filled by CI env vars)
# *.translation IS committed (compiled from data/strings/strings.csv)

# Android build artifacts
android/build/build/
android/build/.gradle/
android/build/local.properties
*.apk
*.aab
*.keystore
*.jks

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Secrets — NEVER commit
google-services.json
firebase-config.json
admob-app-id.txt
*.env
.env.*

# Phase 0 raw data (keep summaries, not personal survey responses)
research/raw/
research/*.csv
research/*.xlsx

# Phase 0.5 corpus-derived data (frequency analysis only, not shipped)
research/word_pool_feasibility_data/

# User's local progress-tracking file
status

# Intermediate curation outputs: keep words_hi.json + words_en.json (shipped),
# but the excluded log is local-only (transparency record, not for production)
data/words_hi_excluded.json
